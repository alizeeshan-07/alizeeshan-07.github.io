# -- Site Configuration -- https://blowfish.page/docs/getting-started/
theme = "blowfish"
baseURL = "https://alizeeshan-07.github.io/"
defaultContentLanguage = "en"
enableRobotsTXT = true
summaryLength = 30
enableEmoji = true

[pagination]
  pagerSize = 10

[taxonomies]
  tag = "tags"
  category = "categories"

[outputs]
  # JSON is required by Blowfish's search — it builds /index.json as the search index.
  home = ["HTML", "RSS", "JSON"]
